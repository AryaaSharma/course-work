#include <bits/stdc++.h>
using namespace std;

int main(){
  // A variable can be considered as a container storing the value of a given entity which is changeable. In C++, to delare a variable, we first declare its datatype and then name of variable.

  int a;
  float b;
  double c;
  char d;

  // For more information on datatypes, check out: https://i.stack.imgur.com/wM1Cu.png

  string e; // A string is an array of multiple characters.
  
  /* 
  INPUT | OUTPUT 
  In C++, we take input using the cin keyword and output using cout.
  */

  cin>>a>>b>>c>>d>>e;
  cout<<a<<" "<<b<<" "<<c<<" "<<d<<" "<<e; // \t stands for tab-space

  return 0;
}